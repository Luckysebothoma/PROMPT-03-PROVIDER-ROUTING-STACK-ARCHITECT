services:
#  postgres:
#    image: postgres:16-alpine
#    container_name: ${COMPOSE_PROJECT_NAME:-ai_gateway_3_of_10}_postgres
#    environment:
##      POSTGRES_DB: ${POSTGRES_DB}
#      POSTGRES_USER: ${POSTGRES_USER}
#      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
#    volumes:
#      - pgdata:/var/lib/postgresql/data
#      - ./db/init:/docker-entrypoint-initdb.d:ro
#    healthcheck:
#      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
#      interval: 5s
#      timeout: 5s
#      retries: 10
#    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: ${COMPOSE_PROJECT_NAME:-ai_gateway_3_of_10}_redis
    command: ["redis-server", "/usr/local/etc/redis/redis.conf"]
    volumes:
      - ./redis/redis.conf:/usr/local/etc/redis/redis.conf:ro
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ${COMPOSE_PROJECT_NAME:-ai_gateway_3_of_10}_api
    env_file:
      - .env
    ports:
      - "4405:8080"
    networks:
      - ai_gateway_shared_net
      - databases
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://127.0.0.1:8080/health', r => process.exit(r.statusCode===200?0:1)).on('error', () => process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    restart: unless-stopped

#  prometheus:
#    image: prom/prometheus:latest
#    container_name: ${COMPOSE_PROJECT_NAME:-ai_gateway_3_of_10}_prometheus
#    volumes:
#      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
#      - promdata:/prometheus
#    ports:
#      - "9096:9090"
#    depends_on:
#      - api
#    restart: unless-stopped

#  grafana:
#    image: grafana/grafana:latest
#    container_name: ${COMPOSE_PROJECT_NAME:-ai_gateway_3_of_10}_grafana
##    environment:
#      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
#      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-admin}
#    volumes:
#      - ./grafana/provisioning:/etc/grafana/provisioning:ro
#      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro
#      - grafanadata:/var/lib/grafana
#    ports:
#      - "3006:3000"
#    depends_on:
#      - prometheus
#    restart: unless-stopped

volumes:
  pgdata:
  redisdata:
  promdata:
  grafanadata:

networks:
  ai_gateway_shared_net:
    external: true
  databases:
    external: true
